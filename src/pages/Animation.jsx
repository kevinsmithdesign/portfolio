import React from "react";
import { Box, Typography, Container } from "@mui/material";

const Animation = () => {
  return (
    <Container sx={{ mt: 8 }}>
      <Typography>Animations Coming Soon!</Typography>
    </Container>
  );
};

export default Animation;
